# IMPORTANT

The data within this folder is currently outdated, and can now be automatically generated upon launching the web app.

That being said, the sql scripts will remain as it will be useful for implementing future features, as well as clearing data if tables get updaded (see template_data.sql)